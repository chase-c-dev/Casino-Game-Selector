import util.gameSelector;

public class casino {
    public static void main (String[] args) throws InterruptedException {
        gameSelector gameSelector = new gameSelector(); //allows use of game selector class in the main by making an object
        gameSelector.startUP();
    }
}
